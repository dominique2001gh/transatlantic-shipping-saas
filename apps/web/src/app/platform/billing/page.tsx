import { ModulePlaceholder } from '@/components/ui/ModulePlaceholder';

export default function PlatformBillingPage() {
  return (
    <ModulePlaceholder
      title="Billing"
      description="Manage tenant subscriptions and platform billing (Stripe integration comes later)."
    />
  );
}
