import { ModulePlaceholder } from '@/components/ui/ModulePlaceholder';

export default function ManifestsPage() {
  return (
    <ModulePlaceholder
      title="Manifests"
      description="Generate and finalize cargo manifests per container and route."
    />
  );
}
